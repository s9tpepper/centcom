text "Headers:"
border
  expand [axis: "horizontal"]
    vstack
      hstack
        text "header:"
        @textinput (text_change->header_update)
        text "value:"
        @textinput (text_change->value_update)
